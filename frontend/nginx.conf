server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Serve static files and enable SPA fallback
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Optional: increase default body size if needed
    client_max_body_size 10M;
}
